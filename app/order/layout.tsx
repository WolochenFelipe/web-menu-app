import { Prisma } from "@prisma/client";
import { ReactNode } from "react";

export default async function OrderLayout({children} : {children: ReactNode}){
    return(
        <div>
            
        </div>
    )
}